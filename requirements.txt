# PriceHawk has no runtime dependencies (all provided by Home Assistant Core).
# This file exists for CI tooling (actions/setup-python pip cache).

# Development/CI tools
ruff>=0.4.0
mypy>=1.10.0
bandit>=1.7.0
pytest>=8.0.0
pytest-cov>=5.0.0
# HTTP mocking for the Amber aiohttp client failure-path tests. aioresponses
# patches the real aiohttp.ClientSession, so it transitively provides aiohttp —
# which is why conftest no longer mocks aiohttp.
aioresponses>=0.7
