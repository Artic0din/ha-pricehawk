# PriceHawk has no runtime dependencies (all provided by Home Assistant Core).
# This file exists for CI tooling (actions/setup-python pip cache).

# Development/CI tools
ruff>=0.4.0
mypy>=1.10.0
bandit>=1.7.0
pytest>=8.0.0
pytest-cov>=5.0.0
# Snapshot testing for the Amber-vs-GloBird comparison outputs. Vanilla syrupy
# (no HA snapshot extension) — the comparison logic is pure Python and needs no
# HA test harness. Snapshots live in tests/__snapshots__/*.ambr; regenerate
# with `pytest --snapshot-update`.
syrupy>=5.0
